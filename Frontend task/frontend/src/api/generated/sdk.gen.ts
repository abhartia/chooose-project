// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, TDataShape, Client } from '@hey-api/client-fetch';
import type { ListTripsData, ListTripsResponse, ListTripsError, GetTripByIdData, GetTripByIdResponse, GetTripByIdError } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List trips
 * Returns a page of trip summaries. Use the `cursor` from `pageInfo.nextCursor`
 * to fetch the next page. Cursors are tied to the `sort` and filter values
 * used to mint them; changing those parameters mid-pagination invalidates
 * the cursor and yields a 400 `INVALID_CURSOR` error.
 *
 */
export const listTrips = <ThrowOnError extends boolean = false>(options?: Options<ListTripsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListTripsResponse, ListTripsError, ThrowOnError>({
        url: '/trips',
        ...options
    });
};

/**
 * Get a single trip
 */
export const getTripById = <ThrowOnError extends boolean = false>(options: Options<GetTripByIdData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetTripByIdResponse, GetTripByIdError, ThrowOnError>({
        url: '/trips/{id}',
        ...options
    });
};